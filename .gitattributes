# GitHub выбирает основной язык репозитория по объёму кода. Клиент на TypeScript по байтам больше бэкенда на C#,
# а инструмент сборки лога переписки добавил бы в статистику JavaScript, хотя к продукту не относится. Фокус
# проекта — бэкенд, поэтому оба каталога из статистики языков исключены. Атрибут меняет только её: подсветка
# синтаксиса, диффы и поиск по этим файлам работают как прежде.
src/robo-wash-react/** linguist-detectable=false
tools/** linguist-detectable=false
