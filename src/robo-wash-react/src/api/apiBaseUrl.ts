// В разработке переменная пустая, и запросы уходят относительным путём — их перекидывает прокси Vite.
// В Cloud Run клиент и API развёрнуты разными сервисами, поэтому адрес API запекается в бандл при сборке:
// переменная окружения контейнера до готовой статики не доедет.
export const apiBaseUrl = import.meta.env.VITE_API_BASE_URL ?? '';
